# Install LaTeX in Debian Linux Distributions

Run `sudo apt install texlive-full` in terminal to install. check if they successfully show version detail to double-check wether installation is successful:

```bash
xelatex --version
pdflatex --version
lualatex --version
latexmk --version
```